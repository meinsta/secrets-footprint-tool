# Secrets Footprint Assessment Tool - Dependencies
# SECURITY-HARDENED VERSION - MINIMAL DEPENDENCIES

# Web application framework (for webapp version only)
Flask>=2.3.0

# SECURITY NOTE: Cryptography library REMOVED
# The secure version uses ONLY external system tools (ssh-keygen, ssh-add)
# No cryptographic libraries that could potentially decrypt keys

# No other external dependencies required!
# The CLI tool works with Python standard library + system tools for maximum security.

# Optional: For enhanced terminal output (if desired)
# colorama>=0.4.4  # Cross-platform colored terminal output
# rich>=10.0.0     # Rich text and beautiful formatting in terminal

# Development dependencies (uncomment if needed for development)
# pytest>=6.0.0
# black>=21.0.0
# flake8>=3.8.0
# mypy>=0.800
